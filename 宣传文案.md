# 立方·3D设计工坊（Cub3D Editor）项目宣传文案

> 上架名称：**立方3D** ｜ 产品全称：立方·3D设计工坊（Cub3D Editor）
> 定位：一个完全免费、无广告、可离线使用的轻量化跨平台 3D / 2D 场景编辑器开源项目。
> 本文档用于对外宣传项目本身（社区、媒体、潜在贡献者与用户），非应用商店上架文案。

---

## 一、项目一句话

**立方·3D设计工坊**是一个由独立开发者发起的开源项目：用同一套代码，做出能在网页、Windows 与 Android 三端通用、且完全免费、离线可用的轻量化 3D / 2D 场景编辑器，让三维创作回归"人人可上手"。

---

## 二、我们要解决什么问题

专业三维软件功能强大，却往往带着高昂的学习门槛、昂贵的授权费用，以及对账号云服务的强绑定。对大多数只想"把脑海中的三维创意随手画出来"的人而言，这是一道不必要的墙。

立方·3D设计工坊的思路相反：
- **轻量**：不追求工业级参数化建模，专注"画得顺手"的核心体验。
- **免费无广告**：不靠内购、不靠推送，作品与工具都属于用户。
- **离线可用**：无需注册登录，打开即用，创作数据默认留在本地。
- **跨端互通**：同一份 `.json` 工程文件在网页、桌面、手机之间自由流转。

---

## 三、项目的三个独特亮点

### 1. 一套代码，三端同体验
仓库以单一代码库组织，通过 Web（Three.js + WebGL）、PC（Electron）、Android（Capacitor）三套壳共享同一份编辑器逻辑、同一份多语言文案。用户获得的不是"三个各做各的 App"，而是体验高度一致的三端产品——这也是开源协作与长期维护成本低的关键。

### 2. 离线优先，隐私自主
不登录、不上传、不追踪。所有基础功能在本机直接运行，工程以 `.json` 存于设备。对个人创作者与对隐私敏感的用户，这意味着"我的作品始终在我手里"。

### 3. 真正的多语言原生
界面与配套说明书同步支持九种语言（简体中文、繁體中文、English、日本語、한국어、Русский、Español、Français、العربية），切换语言时文案与文档一并变更，而非简单机翻。阿拉伯语等从右到左排版也做了专门适配。

---

## 四、核心能力一览

- **绘制**：画笔、橡皮擦（整体 / 局部）、形状工具
- **对象操作**：移动、旋转、缩放；撤销 / 重做、复制 / 粘贴 / 克隆
- **材质**：预设与自定义颜色、图案 / 纹理贴图
- **文本**：多字体（随语言切换）、横排 / 竖排、粗细调整
- **视图辅助**：坐标轴、网格、地面参考面、线框模式
- **PC 增强**：崩溃恢复、拖放打开、自动保存 / 会话恢复、置顶便签窗
- **Android 增强**：全屏运行、返回键确认、导出 PNG 截图与 `.json` 场景

---

## 五、技术架构（给开发者看的一眼概览）

| 层级 | 技术 |
| :--- | :--- |
| 3D 渲染 | Three.js (r160, ES Module) + WebGL |
| 桌面端 | Electron 43 + electron-builder 26 |
| 移动端 | Capacitor 6（Core / Android / App / Filesystem / Preferences / Share / Splash / StatusBar） |
| 共享层 | `shared/`：九语言说明书源、界面语言文案、Three.js 模块、字体、图标、同步脚本 |
| 许可证 | 木兰宽松许可证 v2（MulanPSL-2）；第三方依赖均为 MIT / OFL，见 `THIRD-PARTY-LICENSES.md` |

工程结构强调"单一源"：所有端共用 `shared/`，由 `tools/sync-shared.mjs` 同步生成各端副本，避免多端文案漂移。

---

## 六、开源与社区

- **许可证**：木兰宽松许可证第 2 版（MulanPSL-2），允许自由使用、修改与再分发。
- **代码仓库**：https://github.com/yushichadao/Cub3D-Editor
- **问题反馈**：https://github.com/yushichadao/Cub3D-Editor/issues
- **参与方式**：Fork 仓库、提交 Issue 或 Pull Request；多语言文案与说明书在 `shared/` 维护后由脚本同步。

我们欢迎以下方向的贡献：新语言翻译、新绘制工具、性能优化、各平台体验打磨、文档完善。

---

## 七、路线图（方向，非承诺）

- 持续打磨三端一致的核心编辑体验
- 扩充素材与导出格式（更多图像 / 模型导出）
- 增强协作与模板生态
- 更多语言与无障碍支持

---

## 八、如何支持这个项目

立方·3D设计工坊完全免费、无广告、离线可用，由独立开发者长期投入维护。如果你觉得它有用：

- **直接使用并分享**给需要的人，就是对项目最大的支持。
- **反馈与共建**：提交 Bug、建议或代码。
- **赞助**：通过爱发电主页 [afdian.com/a/cub3d-editor](https://afdian.com/a/cub3d-editor) 支持，每一份都将用于服务器维护、构建工具链与持续开发。

---

## 九、对外传播素材（可直接用）

### 项目简介（长，约 200 字）
立方·3D设计工坊（Cub3D Editor）是一个开源的轻量化跨平台 3D / 2D 场景编辑器项目，用同一套代码覆盖网页、Windows 桌面与 Android 三端，支持九种语言。它完全免费、无广告、可离线使用：无需注册登录，创作数据默认留在本地。一套 `.json` 工程文件在三端互通，手机起草、电脑精修自由接力。项目基于 Three.js 与 WebGL 构建，借助 Electron、Capacitor 实现原生体验，遵循木兰宽松许可证（MulanPSL-2）开源，欢迎社区共建。

### 项目简介（短，约 80 字）
立方3D：开源、免费、无广告、可离线的轻量化 3D/2D 场景编辑器。一套代码打通网页 / Windows / Android 三端，九语言、工程文件互通。MulanPSL-2 协议，欢迎共建。

### 一句话口号
把三维创作，还给每个人。

### 标签
#开源 #3D编辑器 #跨平台 #离线可用 #免费无广告 #Threejs #独立开发 #九语言
