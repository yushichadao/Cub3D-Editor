# 更新记录 / Changelog

## v1.2.1（2026-08-22）

**本次仅发布 Android APK（Cub3D-Editor-release-1.2.1-universal.apk）。**

- **修复：APK 接入境内备用 IP 更新源**
  - 新增 `android/app/src/main/res/xml/network_security_config.xml`，仅对更新探测使用的境内备用 IP `139.196.104.56` 放行明文 HTTP 流量（其余流量仍强制 HTTPS）。
  - `AndroidManifest.xml` 引用该网络安全配置（`android:networkSecurityConfig`）。
  - 使应用启动时的更新检测可在 GitHub 源不可达时，经国内主站（`cub3d-editor.cn`）与备用 IP（`139.196.104.56`）完成版本探测与 APK 下载，真正实现「靠 IP 远程更新」。
- **优化：更新检测回退链**
  - 客户端更新模块（UPD）依次尝试 GitHub → 国内主站 → 国内备用 IP，任一源可用即完成更新，提升国内网络环境下的更新成功率。
- **同步**
  - 同步法律条款（服务协议 / 免责声明 / 隐私政策）、九语言说明书第 52 章与多语言界面文案，与 Web / PC 端保持一致。

> 安装说明：将 APK 传到手机后直接点击安装，需允许「安装未知来源应用」。已安装旧版的用户可在应用内「设置 → 应用更新」中一键升级（默认开启，可关闭自动检测）。

---

## v1.2.0（2026-08-21）

- 新增：应用更新机制，PC / Android 启动时自动检测新版本，支持一键更新（可在设置中关闭）。
- 新增：九语言说明书第 52 章「应用更新与说明书更新」。
- 新增：服务协议、免责声明、隐私政策新增「软件更新」「说明书内容变化与旧便签处理」条款。
- 修复：管理后台安装包识别按扩展名过滤，排除 `update-doc.json` 元数据被误判为安装包。
