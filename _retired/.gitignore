# ===== _retired/ 归档区忽略规则 =====
# 归档区保留依赖与大文件仅作「以备复用」，均不入库。

# Node 依赖（体积大）
**/node_modules/

# 管理端运行时私有数据（含服务器推送口令等，绝不入库）
admin/data/
admin/pack-state/
admin/.packerrc
admin/server*.log
admin/server*.err.log

# 发布产物 / 更新元数据镜像（安装包体积大，不入库；元数据 update-doc.json / versions.json / version.txt 可选择性入库）
downloads/*.exe
downloads/*.apk
downloads/1.0.0/
downloads/data/
downloads/build-log.txt

# 日志与临时产物
*.log
*.err
*.tmp
