<?xml version="1.0" encoding="utf-8"?>
<!-- 更新源已统一走 https（shared/infra/update-sources.mjs：cn=https://139.196.104.56），
     故不再需要明文放行。保留默认安全策略（强制 HTTPS），消除高版本 Android 明文禁用风险。 -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
