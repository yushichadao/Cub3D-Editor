{
  "name": "cub3d-editor-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Cub3D Editor 更新管理器后端（发布版本、托管安装包、生成 update-doc.json / versions.json / version.txt）",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.1"
  }
}
